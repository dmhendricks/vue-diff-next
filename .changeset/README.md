# Changesets

Version bumps and changelog entries for this package are managed with
[changesets](https://github.com/changesets/changesets).

```bash
npm run changeset
```

That writes a markdown file here describing the bump. Commit it with the change
it describes. Merging the version PR that CI opens is what cuts a release.
