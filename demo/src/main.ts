import { createApp } from 'vue';
import App from './App.vue';
// The library's own stylesheet, imported the same way a consumer would.
import '../../src/assets/scss/index.scss';

createApp(App).mount('#app');
